# Пример файла переменных окружения для production развертывания
# Скопируйте этот файл в .env и заполните реальными значениями
# cp env.prod.example .env
# nano .env

# ============================================
# Настройки подключения к внешнему PostgreSQL
# ============================================

# Хост PostgreSQL
# Варианты:
#   - host.docker.internal (для доступа к хосту из контейнера)
#   - IP адрес сервера (например, 144.31.87.154)
#   - localhost (если используется network_mode: host)
PG_HOST=host.docker.internal

# Порт PostgreSQL (обычно 5432)
PG_PORT=5432

# Имя базы данных
PG_DB=test_db

# Пользователь PostgreSQL
PG_USER=postgres

# Пароль PostgreSQL
# ВАЖНО: Замените на надежный пароль!
PG_PASSWORD=your_secure_password_here

# ============================================
# Дополнительные настройки (опционально)
# ============================================

# Таймаут подключения к БД (секунды)
# PG_CONNECT_TIMEOUT=10

# SSL режим для PostgreSQL
# Варианты: disable, allow, prefer, require, verify-ca, verify-full
# PG_SSLMODE=prefer

# ============================================
# Примечания
# ============================================
#
# 1. Если host.docker.internal не работает, используйте IP адрес сервера:
#    PG_HOST=144.31.87.154
#
# 2. Для получения IP адреса сервера выполните:
#    hostname -I | awk '{print $1}'
#
# 3. Убедитесь, что PostgreSQL настроен для приема подключений:
#    - Проверьте listen_addresses в postgresql.conf
#    - Проверьте pg_hba.conf для разрешения подключений
#
# 4. Не коммитьте .env файл в Git! Он уже добавлен в .gitignore
#
