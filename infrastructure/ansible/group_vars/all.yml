---
# Общие переменные для всех хостов
domain: "example.local"
timezone: "UTC"

# Использование фактов Ansible
# ansible_fqdn - полное доменное имя
# ansible_default_ipv4.address - IP адрес по умолчанию
# ansible_processor_vcpus - количество виртуальных CPU
# ansible_processor_cores - количество ядер процессора
